# Simpluse Web Project

Landing page dan admin tooling untuk Simpluse Web Project.

## Run Locally

1. Install dependencies:
   `npm install`
2. Set environment variables in `.env` if needed.
3. Run the app:
   `npm run dev`
